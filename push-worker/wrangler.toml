name = "idt-push-worker"
main = "src/index.js"
compatibility_date = "2026-01-01"

# Secrets (set via `npx wrangler secret put NAME`, never in this file):
#   VAPID_PRIVATE_KEY_JWK   - the private key from `npx @pushforge/builder vapid`, as a JSON string
#   FIREBASE_PROJECT_ID     - your Firebase project id (grok-4)
#
# Plain vars (safe to keep here, not secret):
[vars]
VAPID_PUBLIC_KEY = "BFm7hMTpB4YCplqVeLTr6zHqEZ5B9M3W6-8a_xXMAt_DdPwNkWLQH8b6GeopIWYKv4NBoq8qyL3e-HTUYz0FL3I" 
VAPID_ADMIN_CONTACT = "mailto:thedreamstory8@gmail.com"
ALLOWED_ORIGIN = "https://joelphiri2939-cpu.github.io"
